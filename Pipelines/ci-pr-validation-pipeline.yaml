name: PR Validation

on:
  pull_request:
    branches: 
      - develop
      - 'release/**'
    types: [opened, synchronize, reopened]

jobs:
  validate:
    name: Validate PR
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4